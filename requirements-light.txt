# Lightweight requirements with pre-built wheels only
# These versions are guaranteed to have binary distributions
pandas==2.0.3
openpyxl==3.1.2
Flask==3.0.0
Werkzeug==3.0.1
gunicorn==21.2.0
numpy==1.24.4

# Force binary wheels only - no compilation
--only-binary=all 